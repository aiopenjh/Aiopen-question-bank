# 📌 인계 및 내일 복원 지점 체크포인트 기록 (2026-09-15)

## 1. 사용자의 핵심 요구사항 (절대 잊지 말 것!)
1. **문제 알람은 요일과 상관없이 매일(월~일 365일) 출제되어야 함**:
   - 설정된 루틴 요일에 종속되지 않고, 매일 오전 8시 / 오후 8시에 문제 알람이 상시 발송되어야 함.
2. **알람 울리기 전 미리 문제 뭐 풀지 누르면 바로 문제가 즉시 AI 출제되어야 함**:
   - 사용자가 알람이 울리기 전이라도 과목을 선택하면, 다른 부가 절차나 확인 팝업 없이 **즉시 AI가 3문제를 생성하여 바로 시험(CBT) 풀이 화면으로 진입**해야 함.
3. **모바일 개발 서버 500 에러 및 런타임 에러 방지**:
   - Hyper-V WSL 가상 IP(`172.28.128.1`) 대신 실제 Wi-Fi IP(`10.2.16.127`)로 메트로 바인딩.
   - 포트 8081 프로세스 자동 정리.

---

## 2. 복원 지점 (Restore Point) 정보
- **커밋 해시**: `078f1a93f44b5dfe73f3ddb424c3be9b6efc1c83` (`078f1a9`)
- **커밋 메시지**: `refactor: clean up dead code, unused props, and establish restore checkpoint`
- **커밋 시점 상태**:
  - `SettingsScreen.tsx`와 `LibraryScreen.tsx`가 단일 파일로 동작하던 안정적인 체크포인트.

### 내일 복원 지점으로 돌리는 명령어 (선택 시 바로 실행 가능):
```bash
git reset --hard 078f1a9
git clean -fd
```

---

## 3. 현재 작업 상태 요약 (2026-09-14 종료 시점)
- 루트 `tsconfig.json` 추가로 VS Code의 React Native/JSX 구문 인식 완료 (컴파일 오류 0건).
- 매일 알람 로직 및 36개 테스트 스위트 100% 통과 (`npm test` 36/36 PASS).
- `StudyMapScreen`에 과목 터치 시 3문제 즉시 출제 연결 완료.

---

## 4. 내일 시작 시 진행 가이드
- 내일 사용자가 오시면:
  - **"복원 지점(`078f1a9`)으로 돌려놓고 다시 시작할까요, 아니면 오늘 정리된 상태에서 바로 이어서 테스트해 보실까요?"** 여쭤보고 사용자의 지시에 맞춰 즉시 롤백 또는 진행합니다.
