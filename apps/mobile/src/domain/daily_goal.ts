// 일일 학습 목표 문항 수 상수. DB 초기값, 설정 화면 입력 범위/기본값,
// 유효성 검사, 버튼 비활성화 조건이 전부 이 값을 기준으로 일치해야 한다.
export const DAILY_GOAL_MIN = 1;
export const DAILY_GOAL_MAX = 30;
export const DAILY_GOAL_DEFAULT = 30; // 신규 사용자 기본값. 기존 사용자가 저장한 목표값은 덮어쓰지 않는다.
